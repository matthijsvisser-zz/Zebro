void order(){
	if(all ids marked as done sending){	
		//END			//the entire populationlist is completed
	}
	else if(myid == marked as done sending || myid =! lowest, unmarked, id){
			receiver();
		}else{
			sender();
		}
}